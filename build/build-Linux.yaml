version: '2'

services:
  jrpc:
    container_name: imgui_nft_app
    #Uncomment this line to use a pre-built container If you do so, comment out the "build" section
    #image: jrpc1/jrpc-linux-buildsystem:latest
    build: 
        context: ./
        dockerfile: Dockerfile
    restart: "no"
    volumes:
      - "..:/imgui_nft_app"
    command: $COMMAND
